{
  "name": "rob-2.0-ai",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && cp -r components services index.html index.tsx App.tsx types.ts metadata.json package.json dist/"
  },
  "dependencies": {
    "react": "^19.1.1",
    "react-dom": "^19.1.1"
  }
}
